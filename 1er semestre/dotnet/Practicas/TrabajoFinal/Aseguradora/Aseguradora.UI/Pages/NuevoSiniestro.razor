@page "/siniestros/ingresar/{Id:int?}";
@inject NavigationManager Navigator;

<ListaSiniestros/>
<Dialogo CerrarDialogo="Cerrar" HasError=@_hasError>
  <IngresarSiniestro CerrarDialogo="Cerrar" Id="@Id" OnError="MostrarError" Visible=@(!_hasError)/>
  <MensajeError Mensaje=@_mensaje Visible=@_hasError/>
</Dialogo>

@code {
    [Parameter] public int Id { get; set; }
    public bool _hasError = false;
    public string? _mensaje = "";
    void Cerrar()
    { 
      Navigator.NavigateTo("/siniestros");
    }
    void MostrarError(string? mensaje){
      _mensaje = mensaje;
      _hasError = true;
    }
}