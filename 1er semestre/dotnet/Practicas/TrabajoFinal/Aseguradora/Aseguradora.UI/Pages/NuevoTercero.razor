@page "/terceros/ingresar/{Id:int?}";
@inject NavigationManager Navigator;

<ListaTerceros/>
<Dialogo CerrarDialogo="Cerrar" HasError=@_hasError>
  <IngresarTercero CerrarDialogo="Cerrar" Id="@Id" OnError="MostrarError" Visible=@(!_hasError)/>
  <MensajeError Mensaje=@_mensaje Visible=@_hasError/>
</Dialogo>

@code {
    [Parameter] public int Id { get; set; }
    public bool _hasError = false;
    public string? _mensaje = "";
    void Cerrar()
    { 
      Navigator.NavigateTo("/terceros");
    }
    void MostrarError(string? mensaje){
      _mensaje = mensaje;
      _hasError = true;
    }
}