@page "/titulares/ingresar/{Id:int?}";
@inject NavigationManager Navigator;
<PageTitle>Nuevo titular</PageTitle>

<Dialogo CerrarDialogo="Cerrar" HasError=@_hasError>
  <IngresarTitular CerrarDialogo="Cerrar" Id="@Id" OnError="MostrarError" Visible=@(!_hasError)/>
  <MensajeError Mensaje=@_mensaje Visible=@_hasError/>
</Dialogo>
<ListaTitulares/>

@code {
    [Parameter] public int Id { get; set; }
    bool _hasError = false;
    string? _mensaje = "";
    void Cerrar()
    { 
      Navigator.NavigateTo("/titulares");
    }
    void MostrarError(string? mensaje){
      _mensaje = mensaje;
      _hasError = true;
    }
}